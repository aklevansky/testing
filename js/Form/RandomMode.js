import lodashTemplate from 'lodash.template';
import langSelectTemplate from '../../resources/templates/randomModeWindow.html';

let randomVoiceList = [];

export {randomVoiceList, createRandomModeWindow}


function createRandomModeWindow(supportedVoices) {
	console.log(supportedVoices);
}